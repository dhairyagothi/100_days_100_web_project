const mongoose = require('mongoose');
const aboutApp = mongoose.Schema({
  appName: String,
  version: String,
  support: String,
  contactUs: String,
  website: String
})
module.exports = mongoose.model('about', aboutApp)