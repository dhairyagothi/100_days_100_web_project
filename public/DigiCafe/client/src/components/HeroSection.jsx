export default function HeroSection() {
  return (
    <div className="hero-section">
      <h1 className="tagline">Bridge peers, not servers</h1>
      <p className="description">
        Peer-to-peef file sharing, messaging, and video calls. Your data flows directly between devices — no servers in between.
      </p>
    </div>
  )
}
